SQL Functions

"A reusable block of logic that takes input, performs some operation, and returns a value"

In real world data won't be clean by default to do so we have function and also some functions which helps
to avoid the manual task by built in code block known as functions.

There are mainly 2 types 
1. Single Row Functions
2. Multi Row Functions

  source/singleRowFunctions/typesOfFunctions.png

1. Single Row Functions
The functions which work with single value/row 
EX: now(),date(),abs(),upper(),length(),round() etc...

Here also we have further 4 categories
a. String functions
b. Numeric functions
c. Date functions
d. Null functions

a. String functions:

source/singleRowFunctions/stringFunctions.png

i. CONCAT: to combine multiple string values into one single value

    